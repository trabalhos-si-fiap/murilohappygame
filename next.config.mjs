/** @type {import('next').NextConfig} */
const nextConfig = { 
    images: {
        domains: ['unsplash.com', 'images.unsplash.com', 'media.licdn.com', 'adm.escolagames.com.br', 'www.coquinhos.com', 'www.digipuzzle.net'],
    },
};

export default nextConfig;
